/* Unfinished; must include typedefs for addressing range. Larger memory tracking can be written to a file
on disk if RAM isn't larger enough; example: mem > 2 OR 4 GB, swap memory above to file so less RAM needed.
*/
unsigned int RAMseg1[0x1D1A94A2000];
unsigned int RAMseg2[0x3A352944000];
unsigned int RAMseg3[0x574FBDE6000];
unsigned int RAMseg4[0x746A5288000];
